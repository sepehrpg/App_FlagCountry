<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.infoflgs">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/flag2"
        android:label="جهان ما"
        android:roundIcon="@drawable/flag2"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".Act_Search"></activity>
        <activity android:name=".Act_passForm" />
        <activity android:name=".Act_SplashScren">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DisPlayVerticalProduct" />
        <activity android:name=".Act_ShowData" />
        <activity android:name=".Act_Home" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>